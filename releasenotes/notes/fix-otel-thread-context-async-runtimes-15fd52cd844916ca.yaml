---
fixes:
  - |
    tracing: Fixes an issue where the OpenTelemetry thread context reports a stale span for code
    running on asyncio or uvloop event loops, gevent greenlets, or AnyIO worker threads,
    causing consumers of the thread-context record to associate work with the wrong trace and span.
