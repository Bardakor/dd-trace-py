# The scenario imports EvaluationContext, FlagEvaluationDetails, and HookContext directly.
# This test-only dependency mirrors the openfeature test environment because the benchmark
# base does not otherwise install it.
openfeature-sdk~=0.8.0
